<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Let's Tech About It</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous" />
  <link rel="stylesheet" href="/css/index.css">
</head>

<body>
  <header class="display-flex fixed-top justify-space-between align-center p-1 text-light bg-dark">
    <nav class="navbar navbar-dark">
      <ul class="nav mr-auto mt-2 mt-lg-0 ">
        <li class="nav-item active">
          <a class="nav-link" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Dashboard</a>
        </li>
        <li class="nav-item">
          {{#if loggedIn}}
          <a id="logout" class="nav-link">Logout</a>
          {{else}}
          <a href="/login" class="nav-link">Login</a>
          {{/if}}
        </li>
      </ul>
    </nav>
    <h1 class="color">Let's Tech About It</h1>
    <p>An MVC Web Developer Blog designed to connect and create.</p>
  </header>
  <main class="container container-fluid mt-5 ">
    <!-- Render the contents of the sub layout here -->
    {{{ body }}}
  </main>
  <footer>
    {{#if loggedIn}}
  <script src="\public\js\logout.js"></script>
  {{/if}}
  </footer>
</body>

</html>