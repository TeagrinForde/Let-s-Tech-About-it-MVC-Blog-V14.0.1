{{post.postTitle}}


<div class="row">
    <div class="row blogContent">
        <textarea class="form-control form-control-sm" type="text" placeholder="Add a Comment"></textarea>
    </div>
    <button type="button" class="btn btn-dark rePostBlog">Post Comment</button>
</div>

{{#each comments as |comment|}}
<p class="comment">Comment by {{comment.post.user.username}} </p>
{{/each}}


<script src="/js/comment.js"></script>
